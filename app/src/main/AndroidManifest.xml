<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- targetSdk 30+ 패키지 가시성: 내장 스캐너 action 해석(resolveActivity)을 위해 선언. -->
    <queries>
        <intent>
            <action android:name="cn.moziware.barcodescanner.action.SCAN_BARCODE" />
        </intent>
        <intent>
            <action android:name="com.realwear.barcodereader.intent.action.SCAN_BARCODE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.Glass">

        <!-- 홈 대시보드 = 런처(앱 진입점). -->
        <activity
            android:name=".DashboardActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 입고 작업 화면(홈 '입고' 타일로 진입). -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden"
            tools:ignore="LockedOrientationActivity" />

        <activity
            android:name=".StockListActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden"
            tools:ignore="LockedOrientationActivity" />

        <activity
            android:name=".OrderListActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden"
            tools:ignore="LockedOrientationActivity" />

        <activity
            android:name=".PickingActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden"
            tools:ignore="LockedOrientationActivity" />
    </application>

</manifest>
